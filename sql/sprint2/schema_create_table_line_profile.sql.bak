-- Create table line_profile, which ties profiles to line settings
-- This SQL script creates the table.

DROP TABLE IF EXISTS STARNUMBER.LINE_PROFILE;

CREATE TABLE  STARNUMBER.LINE_PROFILE (
  line_profile_id serial primary key,
  line_profile_name VARCHAR(256),
  is_default BOOLEAN,
  user_id int REFERENCES STARNUMBER.USERS (USER_ID),
  is_active BOOLEAN
   
);

COMMENT ON TABLE STARNUMBER.LINE_PROFILE IS 'This table determines call logic';

